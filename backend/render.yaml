# Render Blueprint for Backend Deployment
# Use this for one-click deployment on Render.com

services:
  - type: web
    name: collabx-backend
    env: python
    region: oregon
    plan: free
    branch: claude/fix-code-errors-011CUfynaUWtyHwn2zNHK2GM
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: OPENAI_API_KEY
        sync: false  # Must be set manually in Render dashboard
      - key: SUPABASE_URL
        value: https://xiljhvtsanncqpjaydor.supabase.co
      - key: SUPABASE_KEY
        sync: false  # Must be set manually in Render dashboard
      - key: PYTHON_VERSION
        value: 3.11.0
    healthCheckPath: /health
